<?php
header('Content-Type: application/json');
$response = file_get_contents('php://input');

try {
  $output = $this->procesWebhook($response);

  die($output);
} catch(\Exception $e) {
  die($e->getMessage());
}


